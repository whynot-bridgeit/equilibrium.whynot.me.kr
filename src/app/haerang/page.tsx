import HaerangGame from '@/components/HaerangGame';

export const metadata = {
  title: '해랑한국어 학습 게임',
  description: '자질 문자 원리 + 신체 기억(Kinesthetic) + 가변적 보상(RPE)으로 배우는 한글',
};

export default function HaerangPage() {
  return <HaerangGame />;
}
